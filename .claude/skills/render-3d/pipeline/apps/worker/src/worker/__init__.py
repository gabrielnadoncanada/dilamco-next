"""Dilamco Blender render worker.

Public entry points:
    worker.pipeline.KitchenBuilder — orchestrates spec → render
    worker.cli.main                — CLI entry (`python -m worker render ...`)
"""

from __future__ import annotations

__version__ = "0.1.0"
